# --- Web framework ---
# Versi ini yang benar-benar teruji: Chainlit menaikkan FastAPI dari 0.115
# ke 0.141, jadi pin lama menghasilkan lingkungan berbeda dari yang diuji.
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32

# --- Database ---
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0
pgvector==0.3.6

# --- Configuration & validation ---
pydantic==2.13.5
email-validator==2.2.0

# --- Auth ---
pyjwt==2.13.0
bcrypt==4.2.1

# --- Pemrosesan sinyal rPPG ---
# open-rppg membawa JAX; pemanggilan pertama mengompilasi (~20 detik),
# berikutnya ~1 detik. Model dipanaskan saat startup.
open-rppg==0.1.1
# Varian headless: opencv-python biasa butuh libGL yang umumnya tidak ada
# di VPS/container tanpa GUI.
opencv-python-headless==5.0.0.93
numpy==2.5.2
scipy==1.18.1
# open-rppg masih memakai pkg_resources, yang dihapus di setuptools >= 81.
setuptools==80.10.2

# --- Deteksi anomali (model dari devfest-ml) ---
scikit-learn==1.9.0
pandas==3.0.5
joblib==1.6.0

# --- Chatbot (Fase 2) ---
# DeepSeek dipakai lewat antarmuka kompatibel-OpenAI; ganti penyedia cukup
# menukar LLM_BASE_URL dan LLM_MODEL.
langchain==1.3.14
langchain-openai==1.4.1

# --- Testing ---
pytest==8.3.4
httpx==0.28.1
